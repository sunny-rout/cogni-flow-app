export const CONFIG = Object.freeze({
  BASE_URL: import.meta.env.VITE_BACKEND_URL || "http://localhost:8080",
  APP_NAME: import.meta.env.VITE_APP_NAME || "cogni_flow_app",
  DEFAULT_USER_ID: "user_03011315",
})
